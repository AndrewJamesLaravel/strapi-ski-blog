query Category($id: ID!){
  category(id: $id) {
    name
    articles {
      _id
      title
      content
      image {
        url
      }
      category {
        _id
        name
      }
    }
  }
}
